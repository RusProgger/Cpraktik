#include <stdio.h>
// либо подключить библиотеку (так лучше и эффективней)
#include <stdbool.h>


int main() {

    printf("%s\n", (10 > 2) ? "true" : "false");

    printf("----------------\n");
    return 0;
}