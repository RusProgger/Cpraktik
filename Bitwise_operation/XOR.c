#include <stdio.h>

int main() {

    // ^ Это XOR, или «исключающее ИЛИ».

    // Если значения отличаются → результат 1. Если одинаковые → 0.

    printf("5 ^ 3 = %d\n", 5 ^ 3);
    


    return 0;
}