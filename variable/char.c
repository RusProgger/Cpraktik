#include <stdio.h>

int main() {

    // символ
    char letter = 'A';

    // вывод символа

    printf("Symbol: %c\n", letter);

    // массив символов
    char letters[] = "B";
    printf("String: %s\n", letters);

    return 0;
}